device: cpu
batch_size: 2
epochs: 2
num_workers: 0
amp: false
grad_accum_steps: 4
data_fraction: 0.05
patch_size: [64, 64, 64]
mode: debug
artifacts_minimal: true

# Classification
resnet_frozen: true   # only inference baseline
swin_finetune: false  # skip fine-tuning on CPU
